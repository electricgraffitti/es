<%= form_for(@game) do |f| %>
	<%= render :partial => 'layouts/error_block', :locals => {:object => @game} %>

  <div class="actions">
    <%= f.submit "Start Game", :class => "btn" %>
    <%= link_to 'Cancel', games_path, :class => "cancel_link" %>
  </div>
<% end %>
